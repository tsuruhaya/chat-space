.side-bar
 .side-bar-header
  .side-bar-header__box
   %p.side-bar-header__box__name
    = current_user.name
   %ul.side-bar-header__box__item
    %li.side-bar-header__box__edit-user
     = link_to new_group_path do
      %i.fas.fa-edit
    %li.side-bar-header__box__new-group
     = link_to edit_user_path(current_user) do
      %i.fas.fa-cog

 .group-list
  - current_user.groups.each do |group|
   .group-lists
    = link_to group_messages_path(group) do
     %p.group-lists__list-name
      = group.name
     %p.group-lists__message
      まだメッセージがありません。